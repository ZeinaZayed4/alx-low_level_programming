#include "main.h"
#include "<stddef.h>"

/**
 * _abs - computes the absolute value of an integer
 * @n: checked integer
 *
 * Return: the absolute value
 */


int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
